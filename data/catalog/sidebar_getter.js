let sidebar_getter_namespace = {
    /** Get sidebar for later use
     * 
     */
    getSidebar: function () {
        let sidebar = document.querySelector('.sidebar');
        return sidebar;
    }
};